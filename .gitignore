# build
/dist/
/target/
/bin/

# deps
node_modules/

# local
.DS_Store
*.log
.env
.env.local

# test artifacts
/coverage/
*.tmp

# 用户真实配置与密钥，绝不入库
/tmp/
*.bak
providers.json

# 构建产物。装配清单里只有本仓库的模块（发行版是另一个 Go module，见
# docs/09-extension-guide.md §8），所以这里不再有「另一个仓库的 checkout」。
/go/bin/
/go/dist/
